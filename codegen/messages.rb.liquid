# This file is autogenerated by {{ template }}

# Root module
module LibRubyParser
  # Module with all known DiagnosticMessage sub-types
  module Messages
    {%- for message in messages %}
    {{ message.comment | render_comment: "#", 4 }}
    class {{ message.camelcase_name }} < DiagnosticMessage
      {%- for field in message.fields %}
      {%- case field.field_type -%}
      {%- when "Str" -%}
        {%- assign return_type = "String" -%}
      {%- when "Byte" -%}
        {%- assign return_type = "Integer" -%}
      {%- else -%}
        {%- assign return_type = "ERROR" -%}
      {%- endcase %}
      {{ field.comment | render_comment: "#", 4 }}
      #
      # @return [{{ return_type }}]
      attr_reader :{{ field.snakecase_name }}
      {%- endfor %}
    end
{% endfor %}
  end
end
